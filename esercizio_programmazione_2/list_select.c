#include "list_select.h"

/**
 * Crea una nuova lista composta dai dati contenuti nella lista 
 * puntata da data_head che si trovano nelle posizioni fornite
 * tramite la lista puntata dal pos_head 
 * @param [in] data_head puntatore alla lista che contiene i dati in input
 * @param [in] pos_head puntatore alla lista con le posizioni da cui prendere i dati
 * @return puntatore alla lista creata dinamicamente composta dai dati alle
 * posizioni fornite in input, se una delle due liste è vuota ritornare NULL
 * Le posizioni fornite sono comunque valide per i dati in input e non serve
 * implementare controlli per verificare che tale vincolo sia rispettato
**/
struct Node* list_select(struct Node* data_head, struct Node* pos_head)
{
    return NULL;
}
